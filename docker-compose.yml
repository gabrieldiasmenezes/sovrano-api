version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: sovrano_db
      POSTGRES_USER: sovrano_user
      POSTGRES_PASSWORD: sovrano_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
